export const environment = {
  // Indica si estamos en modo producción (false = entorno de desarrollo)
  production: false,

  // URL base del backend al que hará las peticiones la aplicación
  apiUrl: 'http://localhost:8445',

  // Clave API si se necesitara (vacía por ahora)
  apiKey: '',
};